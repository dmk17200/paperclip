<h1>New outfit</h1>

<%= form_for @outfit, :url => outfits_path, :html => { :multipart => true } do |form| %>
  <%= form.file_field :photo %>
<% end %>

<%= render 'form' %>

<%= link_to 'Back', outfits_path %>
